kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: keycloak-admin
  namespace: keycloak
spec:
  host: kc-admin.apps.cluster-m5pwr.dynamic.redhatworkshops.io
  to:
    kind: Service
    name: keycloak-service
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
  